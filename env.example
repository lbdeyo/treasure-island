# Copy to .env.local and fill in values. Do not commit .env.local.
# Mailchimp newsletter signup (Contact page)
MAILCHIMP_API_KEY=your-api-key-including-datacenter-suffix
MAILCHIMP_AUDIENCE_ID=your-audience-id
MAILCHIMP_DC=us6
# Set to true to use double opt-in (Mailchimp sends a confirmation email)
# MAILCHIMP_DOUBLE_OPT_IN=true
